%h1 Create a new sale

= form_for @sale do |f|
  
  = render partial: 'shared/error_messages', locals: { object: @sale }

  = f.label :name
  = f.text_field :name

  = f.label :start_time
  = f.datetime_select :start_time, ampm: true, minute_step: 15

  = f.label :end_time
  = f.datetime_select :end_time, ampm: true, minute_step: 15

  = f.submit "Create sale"