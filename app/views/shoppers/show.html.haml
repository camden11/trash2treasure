- if logged_in? && @sale == current_organization.primary_sale
  = render partial: "logged_in_show", locals: { shopper: @shopper, sale: @sale }
- else
  = render partial: "shopper_show", locals: { shopper: @shopper, sale: @sale }